# 에이전트 인수인계 (MAC ↔ RP5#2)

맥과 RP5#2(라우터 서버, 1 TB SSD)에서 각각 일하는 에이전트가 이 문서 하나로 소통한다. 규칙은 `CLAUDE.md`.
항목은 지우지 않고 아래에 덧붙인다. 형식 `- [YYYY-MM-DD HH:MM MAC|RP5] 내용`.

## 1. 현재 상태

- [2026-09-17 23:20 MAC] P0·P1 완료(커밋 48a3480 이후). `router-server`가 protocol v3 ingest·게이트웨이 표·NACK·패치 저장소·상태 API·에뮬레이터 링크를 제공. 검증 내역은 `README.md` "검증" 절. 다음은 P2(WS 다채널 출력 + 뷰어).
- [2026-09-17 23:20 MAC] RP5#2 는 아직 설치되지 않음. 설치 절차는 `docs/RP5-DEV.md`, 스크립트는 `scripts/pi-dev-setup.sh`. aarch64 Linux 컴파일은 맥에서 `cargo check --target aarch64-unknown-linux-gnu` 로만 확인했고 실기 빌드·실행은 RP5#2 에이전트가 처음 하게 됨 — 결과를 3절에 적어 주세요.

## 2. 환경·주의

- 에뮬레이터(RP5#1 `dlake`, 192.168.0.125:5445)의 `transport.target_ip/port` 는 라우터가 준비되기 전까지 바꾸지 않는다. 바꿀 때는 에뮬레이터 저장소 HANDOFF 에 먼저 적는다.
- 맥은 방화벽이 수신을 막고 있어 RP5#1 → 맥 라우터 경로는 사용자가 방화벽을 열기 전까지 불가. 맥에서의 검증은 로컬 에뮬레이터 복제본(`run.py --data-dir <별도 디렉터리> --port 5446`)으로 한다.
- 에뮬레이터는 페이스마크(ch 10)를 같은 패치·같은 seq 의 두 번째 레코드로 보낸다. 라우터는 `continuation_records` 로 세고 중복으로 보지 않는다(`ingest.rs`). 이 동작을 바꾸려면 에뮬레이터 저장소와 같이 맞춘다.
- 저장 용량: 2000 환자 전 채널 raw 기준 약 8 GB/h(gzip 전). `ROUTER_STORE_MAX_GB` 로 상한을 두고 오래된 시간 파일부터 지운다. 보존 정책(전 채널 vs ECG만)은 사용자 결정 대기.
- 미결: 파이썬 분석 목업(`analysis-server/`) 유지 여부, 웹 화면(P2/P3) 재작성 범위.

## 3. RP5#2 → MAC 전달 사항

- (RP5#2 에이전트가 여기에 추가)
- [2026-09-18 00:20 RP5] RP5#2(hostname `rp5ai`) 첫 실기 빌드·실행 완료. `WITH_NODE=0 scripts/pi-dev-setup.sh` 그대로 통과(릴리스 빌드 1분 21초, 테스트 6/6). **SSD 없음 — SD 카드(476 GB) 하나뿐**이라 시험 실행은 `ROUTER_STORE_MAX_GB=20`. 1 TB SSD 전제(8 GB/h)는 이 장비에서 성립하지 않으니 보존 정책 결정 때 반영 필요.
- [2026-09-18 00:20 RP5] 에뮬레이터 `transport.target_ip` 가 사용자 승인으로 이 장비를 향함: 처음 192.168.0.56(wlan0) → 현재 **192.168.0.209:9100 (eth0 유선)**. 라우터는 `0.0.0.0:9100`/`:7300` 에서 실행 중(상태 API 는 9200 이 아니라 7300).
- [2026-09-18 00:20 RP5] 실부하 버그 2건 수정: (1) 에뮬레이터가 재배포로 재시작해 게이트웨이 seq 가 0 부터 다시 시작하자 `track_seq` 가 이후 모든 프레임을 `Reorder` 로 판정(20 s 동안 프레임 63,618 = reorder 63,618, 갭 감지·NACK 사실상 정지). 1024 넘게 뒤로 가면 재시작으로 보고 기준 재설정(`seq_restart`, 패치 seq 도 동일). (2) 에뮬레이터가 경로를 .56 → .209 로 옮긴 뒤 FIN 없이 사라진 소켓 1,880개가 ESTABLISHED 로 남음 → TCP keepalive(30 s/10 s×3). 리스너 백로그 128 → 4096(재시작 시 커널 `Possible SYN flooding on port 9100` 경고 해소).
- [2026-09-18 00:20 RP5] 수정 후 실측(2,190 GW, 99 s): 연결 2190 / 백로그 4096 / SYN 경고 0 / CPU 10~12 % / RSS 130 MB / nack·resend_lost·silent·dup_gw 0 / 이상 카운터 `patch_seq_reorder 14`(재접속 직후 SAF 재생분, 증가 없음). 참고: 에뮬레이터 송신량이 재배포 후 약 3.2k 프레임/s·0.7 MB/s 로 이전(약 5k/s·3 MB/s)보다 낮음 — 에뮬레이터 쪽 설정 변화로 보임.

- [2026-09-18 00:35 RP5] 재시작 판정 보완: 에뮬레이터가 짧은 간격(직전 실행 약 150 s, 게이트웨이 seq ≈ 750)으로 다시 재시작하자 새 seq 가 1024 기준 안쪽이라 150 s 가량 전 프레임이 다시 `Reorder` 로 집계됨. **새 소켓의 첫 프레임이 이전 seq 보다 뒤면 거리와 무관하게 재시작**으로 보도록 수정(SAF 재생은 이전 seq 뒤로 이어지므로 영향 없음). 라우터 재시작 후 71 s: 연결 389(에뮬레이터 환자 500명 규모), 이상 카운터 0.
- [2026-09-18 00:35 RP5] 실시간 소통은 에뮬레이터 채팅 채널을 씀(Mac 이탈 후 체제, 에뮬레이터 측 seq 12~15). RP5-2 는 이름 `rp5-2` 로 `ws://192.168.0.125:5445/ws/chat` 에 상시 접속. 상태 API 포트는 7300.

- [2026-09-18 01:45 RP5] P2 완료 + 알람 + 웹 콘솔 (커밋 참조). 라우터 WS 는 이제 v2 프레임(0xB2): items[].waves 레이아웃 + i16 블롭(ECG·가속도·PPG·호흡파형), items[].vitals(hr/temp/resp/spo2/glucose), pace, flags/battery/rssi. 뷰어는 첫 바이트로 v1/v2 구분. 알람: `alarms.rs`(수치 임계 10 s 지속·전극 탈락 30 s·배터리·패치 15 s 무응답·GW 다운/무응답/저하·저장 백프레셔), REST `/api/alarms*`, WS 의사 그룹 `alarms`, 상태 보고에 `alarms` 요약 포함. 에뮬레이터 EMR 프록시 `/api/emr/*`, `/api/emu/status|discovery`(TTL 캐시; 에뮬레이터에 CORS 가 없어 브라우저는 라우터만 봄).
- [2026-09-18 01:45 RP5] 웹 콘솔 `web/console`(React/Vite, 의존성 react 만): 대시보드(초당 프레임/바이트 = /api/stats 차분)·환자 표(1,000~2,500행, 검색/정렬/필터, 행 클릭 → 상세 모달: 전 파형·가속도·EMR 프로필·저장 인덱스·알람)·실시간 그리드(병동/GW 선택, 최대 48장, subscribe_channels 합집합)·병원 지도(에뮬레이터 layout JSON 폴리곤 + 게이트웨이 상태 + 환자 점 + 알람 색)·게이트웨이 표·알람(확인/이력/규칙 편집)·이벤트. `npm run build` 산출물을 라우터가 `/` 로 서빙(`ROUTER_WEB_DIR`). **브라우저 실검증은 못 함** — 이 장비의 headless chromium 이 http 페이지 로드를 시작조차 못 함(네트워크 이벤트 0, file:// 만 됨). 대신 `npm run smoke`(react-dom/server 로 전 페이지 렌더) 통과. 사용자가 http://192.168.0.209:7300/ 를 열어 보는 단계.
- [2026-09-18 01:45 RP5] 배포 준비 `deploy/pi/`: `biomonitor-router.service`(User=master, LimitNOFILE 65535), `/etc/biomonitor-router.env`, `99-biomonitor-router.conf`(somaxconn 4096 등), `install.sh`(빌드 → /opt/biomonitor-router, 데이터 /var/lib/biomonitor-router). 아직 설치하지 않음(사용자 결정 대기) — 현재는 `scripts/run-router-pi.sh` 로 백그라운드 실행 중.
- [2026-09-18 01:45 RP5] 관측: `patch_seq_reorder` 는 엘리베이터/복도 게이트웨이 핸드오버 중 두 게이트웨이가 같은 패치를 겹쳐 보낼 때 생기는 정상 현상(이동 환자 1명에서만, `/api/channels[].pseq_reorder` 로 확인). 재구축 직후 수천 건 폭증은 과도기. 알람 규칙 기본값으로 환자 1,000명 중 활성 알람 약 150건(에뮬레이터 심장질환 비율 70 %) — 임계 조정은 규칙 탭에서.

- [2026-09-18 05:10 RP5] 사용자 결정: 저장 상한 **200 GB**(`ROUTER_STORE_MAX_GB=200`, run 스크립트·deploy env 반영, 라우터 재시작). systemd 설치는 나중에 — 계속 `scripts/run-router-pi.sh` 백그라운드 실행.

- [2026-09-18 08:40 RP5] 뷰어 템플릿 시스템 추가(`web/console/src/viewer/`): 에뮬레이터 Central Station(n-up)·단일 침상 뷰어의 CSS/레이아웃 로직(csLayout·프리셋·숫자 보드·알람 색)을 라우터 콘솔로 이식. `#/viewer?tpl=central&gw=…|ward=…|room=…|ids=…` 새 탭 전체 화면. 알람 색은 라우터 알람 엔진(critical/high=적, medium/low=황) 기준. NIBP 는 채널이 없어 `--`. 템플릿 레지스트리 `templates.js` 에 대상별 템플릿을 추가하는 구조. 에뮬레이터 쪽 변경 없음.

- [2026-09-18 08:25 RP5] 저장 큐 드롭 28만 건 발견·수정(c127576): 07:46~08:14 에뮬레이터 재배포 3회 때 게이트웨이 1,772개가 SAF 버퍼를 40프레임 버스트로 재생 → 65,536건 큐 순간 초과. 큐 262,144 + ingest 200 ms 역압(send_timeout) + 열린 파일 핸들 LRU 256→4096(환자 2,000명이면 매초 재오픈). 수정 후 재시작 버스트(84 s에 200 MB)에서 드롭 0. 디스크 자체는 13 % 사용률로 여유.
- [2026-09-18 08:25 RP5] 장기 자원 추적 시작: `scripts/leakwatch.py run`(1분 샘플, data/leakwatch.csv; PSS/RSS/anon·fd·스레드·9100/7300 소켓 상태·표 행 수·NACK 대기·큐 드롭·이벤트/알람 링 크기) 백그라운드 실행 중, `report --hours N` 으로 시간당 기울기. 첫 관찰: 재시작 직후 PSS 167 MB, 40분 뒤 328 MB(환자 2,000명) — 계속 오르는지 추적 중. fd 는 1,772 소켓 + 시간 파일 핸들 ≈ 3,800 에서 안정 예상.

- [2026-09-18 12:10 RP5] 메모리 추적 결과(환자 2,000명, GW 1,772): PSS 기울기 +170 MB/h → 약 +10 MB/h(감속 중). 수정 4건: (1) 패스스루에서도 채널별 분석 대기 버퍼에 패킷을 넣고 seq 재시작 뒤 못 비우던 것(최대 1 GB) d9784bf, (2) glibc 아레나 단편화 → mimalloc bf4d7ad, (3) 알람 last_seen 맵 무한 증가 ac60e84, (4) index.json 2,000개를 60 s마다 한 플러시에 몰아 써서 쓰기 정지 → 패치 버퍼 용량 배증(+31 MB/회) 837177e(플러시당 100개 분산 + 버퍼 축소). 고아 회선·세션 누적 없음(ESTAB = 카운터 = 에뮬레이터 연결 수, CLOSE_WAIT 0, 스레드 8 고정, 이벤트/알람 링 상한 고정). `/api/debug/sizes` 로 내부 구조 크기 확인 가능. 잔여 +10 MB/h 는 mimalloc 워밍업 가능성 — 수 시간 창으로 계속 관찰.

- [2026-09-18 21:40 RP5] 메모리·핸들 추적 종료: 추가 수정 2건 — (5) 레코드 15분 없는 행은 소켓 유지 여부와 무관하게 정리 189c6d5(퇴원·교체 패치가 영구 `connected`+영구 NO SIGNAL 알람으로 남던 것), (6) 저장소 유휴 핸들 닫기가 상한 초과 시에만 돌던 것 → 매 플러시 + 15분 유휴 버퍼 제거 012cbd8(fd +22/h 의 원인). 최종 실측(2 h, 환자 2,000, GW 1,778): PSS 154.3 → 156.3 MB(+1 MB/h, ok), fd 3,782 → 3,785 고정, 행 2,000 → 2,009, 큐 드롭 0, CLOSE_WAIT 0. 샘플러(`scripts/leakwatch.py run`) 계속 가동, `report --hours N` 으로 확인.

- [2026-09-19 03:30 RP5] 추가 발견·수정(7): PSS 계단식 급증(+80 MB/분)의 원인은 저장 큐 스파이크(2~8만 건, 5~10분마다) — SD 카드에서 패치별 파일 2,000개 append 가 0.3~7 s 걸려 쓰기 스레드가 큐 드레인을 막았고, 밀린 레코드 할당이 단편화로 남음. 저장소를 배처(큐 드레인·버퍼·인덱스) + 파일 라이터 스레드(append·index.json·핸들·gzip 트리거)로 분리 d456f69, 플러시 5 s, index.json 10분/플러시당 10개 082a7cd·4428c5b. 결과(2 h, 환자 2,000): ingest 큐 최대 45~390건(이전 5.8만), PSS 154.7 → 161.8 MB(+3 MB/h, ok), 드롭 0. 라이터 배치는 여전히 1.5~3 s(SD 한계)지만 backlog ≤ 1 플러시분. **SSD 로 옮기면 이 부하는 사라짐.**

- [2026-09-19 14:10 RP5] 성능 최적화 4묶음(18750e7·17080b4·da04b30·ed4ae68 + gzip 옵션 7a6c691). 측정(환자 2,000·GW 1,772·10k rec/s·60 s 창, `scripts/cpu_baseline.sh`): **CPU 19.1 % → 7.9 %(1코어), 레코드당 19.0 → 7.9 µs, PSS 250 → 106 MB**; 콘솔형 WS 구독(48채널+알람) 시 11.6 %. 주요 항목: 청취자 없으면 스트림 패킷 생성 생략(구독된 채널만 생성·직렬화), 알람 평가를 행 복제 없이 참조로, 프레임당 저장 배치, 변하지 않은 META(초당 180건 JSON) 파싱 생략, 레지스트리 조회 3→1회, 연결당 읽기 버퍼 64→16 KB, LTO/opt3. `ROUTER_STORE_GZIP=0`(SD): 정각 gzip 이 한 코어 20 % 를 수십 분 쓰고 SD 쓰기 +50 % → 기본 끔. 커널 TCP 메모리는 40페이지(수신 큐 항상 비어 있음)로 무시 가능.
- [2026-09-19 15:40 RP5] 콘솔 네이티브 `<select>` 전부 제거: 실시간(병동·GW)·환자(병동·필터)·게이트웨이(필터)·이벤트(종류)·도면(건물) 드롭다운을 뷰어와 같은 커스텀 `Dropdown` 으로 교체하고 목록에 환자·패치·건수(`countUnit`)를 표기. `npm run smoke` 16건 통과, dist 재빌드(라우터는 dist 를 직접 서빙하므로 재시작 없이 반영).
- [2026-09-19 16:00 RP5] 실시간 '촘촘' 카드를 세로 적층(환자·위치 줄 → ECG 스트립 34 px → HR·SpO₂·RR·T·BAT 한 줄)으로 재구성, 열 최소폭 212 px, 캔버스 300 px·dpr 1.25. 밀도별 표시 상한 normal 48 / 보통 72 / 촘촘 120 (WS 구독도 그만큼 늘어남; 라우터는 구독 채널만 패킷 생성하므로 CPU 영향은 채널 수 비례).
- [2026-09-19 16:20 RP5] 콘솔 체크박스가 캡션과 떨어지던 문제: 전역 `input { min-width: 220px }` 이 체크박스에도 적용됨 → checkbox/radio 는 15 px 고정으로 제외 b82af7b.
- [2026-09-19 17:00 RP5] 실시간 페이지를 스트리밍 부하 시험용으로 재구성: 밀도 × 개수(크게 48/72 · 보통 120/240 · 촘촘 500/1000/2000), 보통 카드는 종전 2/3 크기, 캔버스 비트맵은 화면에 처음 보일 때 할당(2,000 카드도 오프스크린은 메모리 0), 240 초과 시 수치 갱신 1 Hz, 툴바에 성능 줄(WS 프레임/s·KB/s·rec/s·디코드 ms/s·그리기 ms/프레임·fps·캔버스·힙). 라우터 `/api/stats.ws_lagged`(느린 WS 구독자가 건너뛴 메시지) 추가, **라우터 재시작(새 바이너리)**. 실측(python 클라이언트, 2,000 채널 전부 구독): 15 프레임/s · 10.1k rec/s · 5.3 MB/s, 라우터 CPU 51 %(1코어), lag 0; 500 채널: 2.5k rec/s · 1.3 MB/s.
- [2026-09-19 17:30 RP5] 톱 메뉴 '뷰어' 오른쪽에 '테스트' 그룹(커스텀 서브메뉴) 추가, 하위 '멀티 뷰어 테스트'(`#/test/multiviewer`): 병동마다 브라우저 탭을 열어 그 병동 범위의 뷰어 템플릿(중앙 모니터/콘솔 그리드 선택)을 띄움. '모든 병동 탭 열기'(브라우저 팝업 허용 필요, 차단 수 표시) / '다음 병동 열기'(클릭당 1탭) / '열린 탭 모두 닫기', 병동별 열림 상태 표. 탭마다 WS 1세션이 그 병동 채널만 구독.
- [2026-09-19 18:10 RP5] 멀티 뷰어 테스트 페이지에 상태 줄(열린 뷰어 n/병동 수, 표시 중인 환자 파형 수, 라우터 WS 세션·구독 채널, 라우터 CPU(1코어=100)·시스템 CPU, 라우터 RSS·시스템 메모리, WS 지연 건너뜀) 2 s 폴링. 라우터 `/api/stats`에 `cpu_process_percent`(/proc/self/stat, 2 s 창)·`ws_sessions`·`ws_subscribed_channels` 추가, **라우터 재시작(새 바이너리)**. 실시간 카드: 크게 파형 = 수치 블록 높이(비트맵 140), 보통 2/3 크기·수치 108×50 고정·단위 생략, 촘촘 파형 68 px.
- [2026-09-19 20:50 RP5] **뷰어 페이지 재구성 + 라우터 로컬 DB(SQLite)**. (1) 그룹 저장소를 `rusqlite`(bundled) 기반 `ROUTER_DB_PATH`(런처 `data/router.db`, 표 `groups`, WAL)로 옮김; DB 가 비면 예전 `groups.json` 을 1회 가져옴(11개 데모 그룹이 들어왔고 `all` 외에는 멤버 0 — UI 에서 삭제 가능). API 는 그대로 `/api/groups` CRUD, `created_ms/updated_ms` 추가. (2) EMR 동기화가 `/api/v1/emr/patients?status=admitted`(limit 최대 10,000, offset 페이징)도 읽어 `patient.department`(specialty)·`patient.diagnosis`(disease) 를 채움; admissions 가 department 를 비우던 것 수정. 그룹 criteria 키에 `diagnosis` 추가. (3) 콘솔 뷰어 페이지: 병동/병실/담당의/간호사/진료과목/주진단/게이트웨이/그룹 탭 목록(환자·알람·GW 수, 담당의·간호사는 EMR staff 이름), 행 클릭 → 선택 템플릿(중앙 모니터/콘솔 그리드/이후 추가) 새 탭, 행 버튼으로 다른 템플릿. 그룹 탭: 새 그룹/편집/삭제 모달(이름·설명·작성자, 환자 검색 → 추가/빼기, 속성 조건 그룹은 조건 유지). 뷰어 URL 스코프 `doctor= nurse= dept= dx= group= label=` 추가. (4) 라우터 재시작 3회(새 바이너리). 성능: WS 세션 루프가 매 브로드캐스트마다 소켓 읽기를 시도해 tungstenite 가 128 KiB 를 0 으로 채우던 것(21 세션 시 CPU 171 %·memset 82 %)을 읽기 전용 태스크 + 4 KiB 버퍼로 해결 → 23.5 %. `perf`(linux-perf) 설치됨. `/api/stats`: `ws_lagged`·`ws_sessions`·`ws_subscribed_channels`·`cpu_process_percent`.
- [2026-09-19 22:10 RP5] 파형 렌더러 2종 + 설정 페이지. 종전 방식(안티앨리어싱 폴리라인, 전체 재그리기 시 stride 솎기, 프레임 경계 겹침·지우개가 펜 끝을 깎아 두께 불균일/봉우리 유실)은 '속도형'으로 유지, 새 '품질형'은 디바이스 픽셀 열 단위 min–max 채우기(`traceRender.js` ColumnTracer, 프레임당 fill 1회, AA 없음, 지우개는 펜 다음 열부터, 그리드 복원은 정수 픽셀). 뷰어 `Sweep` 과 콘솔 `WaveCard` 모두 적용, 톱 메뉴 맨 끝 '설정'(`#/settings`)에서 선택(localStorage `render.mode`, storage 이벤트로 열린 뷰어 탭에도 즉시 반영, 기본 품질형). 페이스 표식: 레코드당 1회, 재생 시각 지난 뒤 표시, 상단 짧은 눈금. 메뉴 순서 변경(대시보드·알람·이벤트·환자·게이트웨이·병원 지도·뷰어·테스트(실시간·멀티 뷰어)·설정). 뷰어/멀티 뷰어 표: 열 구분선·헤더 가운데·고정 폭·헤더 정렬.
- [2026-09-19 22:40 RP5] 뷰어 페이지에 페이스메이커(플래그 0x10)·MCOT(`patient.mode` ≠ inpatient) 탭 추가: 전체 행 + 해당 환자가 있는 병동별 행, 뷰어 스코프 `paced=1` / `mode=mcot`(+`ward=`). `Patient.mode` 를 admissions.mode 에서 채움(그룹 criteria 키 `mode` 도 가능), **라우터 재시작(새 바이너리)**. 지금 시나리오는 전원 inpatient 라 MCOT 탭은 0명. 품질형 렌더러를 열 단위 min/max 점을 AA 폴리라인으로 1회 긋는 방식으로 교체(계단 제거), 페이스 눈금은 펜이 지난 뒤 표시(반쪽 잘림 해결).
- [2026-09-19 23:05 RP5] MCOT 판정 기준을 RP5#1 안내대로 게이트웨이 메타로 변경: GW `type == "mobile"` 또는 `location.building` 에 원외/MCOT (환자 `mode` 는 보조). 현재 에뮬레이터는 `config.general.outpatient_count = 50` 이지만 상태 outpatients 0 — 환자 세트 재생성(`POST /api/v1/control/rebuild`) 전까지 MCOT 탭은 비어 있음(실행 여부는 사용자 결정 대기).
- [2026-09-19 23:40 RP5] 모바일 GW(1773~, `type mobile`, 건물 `원외(MCOT)`)가 라우터에 잡히기 시작 → 뷰어 MCOT 탭 동작. 원외 환자 위치를 집주소 지역명으로 표기하기 위해 `Patient.home_region/home_address` 추가(EMR `home_region|region|area`, `home_address|address`(문자열 또는 {region|city|district, full}) 를 관대하게 파싱, 없으면 주소 앞 두 단어). 콘솔: 페이스메이커/MCOT 탭의 병동 없는 환자는 지역별 행(`외부 · 지역`), 지역도 없으면 '병동 외부'(패치 id 로 열기); 뷰어 스코프 `region=`; 중앙 모니터 타일 위치에 `외부 · 지역`. **에뮬레이터에 필드 추가 요청을 채팅 채널로 전달**(사용자: 에뮬레이터에서 집주소 포함 예정). 라우터 재시작(새 바이너리).
- [2026-09-20 00:10 RP5] 에뮬레이터가 집주소를 넣어 줌: 환자 상세 `/api/v1/emr/patients/{profile}` 의 `address {sido, sigungu, dong, label}` (목록·admissions 에는 없음). 라우터 EMR 동기화가 원외 환자(모바일 GW 또는 mode≠inpatient)만 골라 상세를 1회 조회(프로필별 캐시, 1 h 갱신, 회당 최대 60건)해 `home_region`="시도 시군구", `home_address`=label 로 채움. 에뮬레이터가 환자 세트를 재생성해 패치 번호가 65537~ 로 바뀌었고 MCOT 50명(모바일 GW 50대) 모두 지역명 표시 확인. 라우터 재시작(새 바이너리).
- [2026-09-20 00:35 RP5] MCOT 뷰어에 파형 없는 카드가 섞이던 원인: 에뮬레이터 환자 세트 재생성 후 이전 MCOT 패치 50개가 '연결됨·수신 없음'(모바일 GW 소켓 유지)으로 15분 정리 전까지 남음. EMR 동기화가 admissions 에 없고 60 s 이상 조용한 패치 행을 즉시 제거하도록 수정(admissions 10건 미만이면 안전상 건너뜀). 라우터 재시작(새 바이너리). 현재 에뮬레이터: 입원 1,500 + MCOT 50 = 1,550, 라우터 행 1,550 전부 수신 중.
- [2026-09-20 01:30 RP5] **저장 파형 이력 뷰어**. 라우터 `GET /api/wave/{id}/waves?from_ms&to_ms`(최대 10분, 바이너리 0xB3: 헤더 JSON + i16 블롭, 세그먼트 = seq 연속·간격 정상 레코드 묶음, fs 는 레코드 간격 중앙값으로 추정, ECG/PPG/호흡파형/가속도 + 페이스 마크). 콘솔 `viewer/History.jsx`: 시간 파일 버튼 + 시간 내 슬라이더, 10/30/60/120 s 창, ◀ ▶ 이동, 재생 1×/4×/16×(space, ←→), 5분 청크 캐시, 열 단위 트레이서로 정적 렌더. 침상 뷰어 'History' 버튼과 상세 창 '이력' 버튼으로 진입. 테스트 › 데이터 관리(`#/test/data`) 페이지: 저장소 현황 + '저장된 파형 전체 삭제'(2단계 확인, `POST /api/wave/reset`). 뷰어 페이지: 병동 탭 건물·층 컬럼 + 건물 서브 버튼, 담당의·간호사 탭 진료과목 서브 버튼, 탭별 정렬 기억. 실시간 페이지 전체 폭. 라우터 재시작(새 바이너리).
- [2026-09-20 02:10 RP5] **WS 출력 경로를 broadcast 링(4096) → 세션별 라우팅 큐(8192)로 교체**. 감시기(`scripts/leakalert.py`, 새로 추가; leakwatch CSV 에 cpu_proc·ws_lagged·ws_sessions·ws_subs 열 추가, CSV 는 `leakwatch-3.csv` 로 회전)가 멀티 뷰어 테스트(21 세션·497 채널) 중 30분에 WS 지연 건너뜀 +36만 건을 잡음. 원인: 모든 세션이 초당 1만 건 전 패킷을 소비해야 했고 MCOT SAF 재전송 같은 수신 버스트(1초 9k+)가 링 4096 을 넘기면 세션들이 한꺼번에 lag. 이제 발행 시 각 세션의 구독(그룹/GW/채널)에 맞는 패킷만 그 세션 큐에 넣고, 가득 차면 그 세션 것만 버림(`ws_lagged`). 실측: 사용자 21 세션 + 파이썬 21 세션(각 24 채널) 동시에 lag 0, 라우터 CPU 20 %(42 세션). 라우터 재시작(새 바이너리). 같은 창에서 PSS +29 MB/h 알림도 있었음 — 뷰어 이력 조회(시간 파일 통째 읽기)와 겹쳐 관찰 계속.
- [2026-09-20 04:10 RP5] 장기 감시 결과(감시기 `scripts/leakalert.py`, 바닥 비교 방식): 데이터 누수 없음, 대신 **일시 할당이 RSS 바닥으로 남는 현상** 3건 확인·조치. (1) History 5분 조회가 시간 파일 전체(3~4 MB)를 읽고 전 레코드를 디코딩 → 범위 밖 건너뛰기(81e8a12) → 파일 스트리밍 읽기(8f11697, 요청당 메모리 레코드 1개 크기, 4~6 ms). (2) 스크롤 시 청크 요청 12개 동시 → 라우터 세마포어 2·콘솔 동시 2(70aa9a9). (3) 03:56 SD 쓰기 스톨로 저장 큐 18k 적체 → PSS 113→181→바닥 129: 재현 시험(verify 16개 동시 = +65 MB 버스트)에서 mimalloc 기본 purge 지연이 바닥 +7~15 MB 를 남김 → 런처/env 에 `MIMALLOC_PURGE_DELAY=0`: 같은 버스트가 최고 +10 MB, 60 s 후 원상 복귀, CPU 영향 없음. 라우터 재시작 4회(새 바이너리·env). 현재 PSS 109 MB, WS 지연 0, 드롭 0.
- [2026-09-20 08:05 RP5] 뷰어 탭 21개를 한꺼번에 새로고침하자 PSS 118 → 199 MB 후 바닥 128 MB. 원인은 세션별 라우팅 큐 깊이 8192 — 재접속 순간(구독 스냅샷 + 스트림)에 세션마다 수 MB 씩 envelope 을 물고 있었음(21 세션 최악 ~170 MB). 큐를 512(24채널 뷰어 기준 약 4 s, 플러시 100 ms·지터버퍼 1 s 대비 충분)로 축소. 검증: 21 세션 접속·해제 3회 반복에 PSS 104 → 107 MB, lag 0, drop 0. 라우터 재시작. 감시기: fd 판정에서 WS 세션 수 변동분 제외(세션당 소켓 1개라 오탐).
- [2026-09-20 08:50 RP5] 42 세션(999채널) 부하에서 WS 지연 건너뜀 2회(+24.5k, +49.3k). 분석: 08:16 은 **Pi 쪽** — 같은 시각 npm 빌드로 CPU·SD 경합 → 저장 큐 16.9k 적체 → 해제 시 버스트로 세션 큐(512) 초과. 08:38 은 **뷰어 PC 쪽** — 라우터 CPU 20 %·시스템 6 %·저장 큐 0 인데도 발생, 탭 42개를 띄운 브라우저가 수 초 멈춘 것(부하 시험의 의미 있는 한계점). 정상 상태에서는 증가 0. 조치: 세션 큐를 파형용(512)과 **알람·멤버십용(1024) 로 분리** — 파형 폭주 중에도 알람 메시지가 버려지지 않음, `/api/stats.ws_ctrl_dropped`(0 이 정상) 추가하고 감시기가 감시. 라우터 재시작. 교훈: Pi 에서 cargo/npm 빌드를 돌리면 저장 스톨·CPU 경합으로 측정이 오염되므로 부하 시험 중에는 빌드 금지.
- [2026-09-20 10:40 RP5] 응답 캐시가 요청마다 본문을 복사하던 것 제거: `/api/channels`(1.2 MB)·`/api/gateways` 1 s 캐시와 EMR 프록시 TTL 캐시가 `Arc<String>` 을 들고 있다가 `body.as_str().to_owned()` 로 매번 복사 — 뷰어 탭 42개가 4 s 마다 폴링하면 라운드당 약 50 MB 할당·복사. 캐시·응답을 `axum::body::Bytes`(refcount) 로 바꿔 복사 0. 42개 동시 폴링 157 ms, PSS 100 → 101 MB. 라우터 재시작. 참고: 뷰어 쪽 정체로 인한 WS 지연은 라우터에 영향 없음(수신 111 MB/분·저장 큐 0 유지 확인), PSS 바닥은 세션 42개 기준 ~137 MB 에서 평평(버스트 최고 수위가 바닥으로 남는 구조, 큐 512칸 × 세션 수로 상한).
- [2026-09-20 13:45 RP5] **뷰어 파형이 2초에 2~3번, 0.2~0.3초씩 멈칫하던 원인 = 브라우저 재렌더링**(전송 아님: 뷰어 1탭 수신 측정 결과 스트림 프레임 5/s·간격 p99 201 ms·300 ms 초과 0건). 원인 3가지와 조치: (1) 알람 WS 메시지 3.1/s 가 매번 App 상태를 갱신 → 화면 전체(침상 타일 전부) 재렌더 → **500 ms 단위로 모아 1회 갱신**. (2) 뷰어 템플릿의 수치 갱신 타이머 250 ms → **1000 ms**(vitals 자체가 1 Hz). (3) 뷰어가 4초마다 전체 채널 목록 1.2 MB 를 받아 파싱(탭 40개면 한 렌더러 스레드에서 초당 10회) → `/api/channels?ward=|room=|gw=|ids=|doctor=|nurse=|dept=|dx=|group=|region=|paced=` **서버 측 필터** 추가, 뷰어는 자기 범위만 요청(1,220 KB → 18 KB, 67배 감소; `registry.snapshot_where`). b/f·mode 스코프는 전체 목록 유지. 라우터 재시작·콘솔 재빌드.
- [2026-09-20 14:45 RP5] History 스트립이 비었다 채워지던 문제 3단계 수정: (1) 롤오버 6 s 뒤 청크 캐시를 **삭제**하던 것 → 삭제 없이 재조회(b5407d8), (2) 5분 경계에서는 끝난 구간의 청크가 즉시 갱신되지 않아 6 s 간 격자만 → 롤오버 즉시 + 6 s 후 2회 재조회(b2b40d4), (3) 그래도 남은 '끝부분 6~7초 공백 → 15 s 뒤 채워짐'은 **저장소 지연**(플러시 5 s + SD 라이터 배치, 스톨 시 10 s+) 때문 → 라이브 스트립이 자기 60초 샘플을 롤오버 때 그대로 넘겨주고(최근 4개 보관), 각 스트립은 저장본과 넘겨받은 본 중 **샘플이 많은 쪽**을 그림. 라이브 중간이 끊겼던 구간은 나중에 저장본이 더 완전해지면 자동으로 저장본으로 대체됨. 저장 데이터 자체는 정상(끝난 60 s 구간 = 15,000 샘플 확인).
- [2026-09-20 15:50 RP5] **운영 통계(장기 24/7 지표) 추가**: `src/metrics.rs` 수집 스레드가 2 s 마다 샘플(원자 카운터·큐 길이·CPU/메모리) → **분당 1행** `metrics_min`, 매시 `metrics_hour` 롤업, 분 행은 14일 보관(연 단위는 시간 행만: 8,760행 ≈ 수 MB), `incidents` 표에 재시작·저장 스톨·큐 드롭·WS 지연·디스크 부족 기록. API `/api/metrics?range=hour|day|week|month|quarter|year`(15 s 캐시), `/api/metrics/info`, `POST /api/metrics/reset`. 콘솔 테스트 › 운영 통계(`#/test/ops`): 구간 선택, 요약 타일(가동/커버리지/CPU/메모리/환자/전송/저장 증가/유실), SVG 차트 6종(CPU·메모리·저장소·환자와 구독·전송량·지연과 장애), 사건 표, 초기화 버튼. 비용 측정(1년치 8,760+20,160행, DB 7.6 MB): 수집 스레드 CPU 0 %대, 조회 day 8 ms·year 45 ms(콜드), 캐시 적중 0.4 ms, 10회 폴링 30건에 40 ms. SQLite 동시 쓰기는 1개만 가능 → 그룹·통계 연결 모두 `busy_timeout=5000` 설정(쓰기는 분당 1회 수준이라 충돌 없음).
- [2026-09-20 19:05 RP5] 18:53 '모든 채널 장애'의 정체 = **저장 스톨이 만든 대량 오탐**: SD 쓰기가 멈추면 프레임 처리가 늦어지고 `SILENCE = 10 s` 판정이 1 Hz 로 일괄 실행되어 게이트웨이 275대가 동시에 `gateway_silent` 알람(레코드 처리량 −9.4 % → 다음 분 +8.5 %, 드롭·유실 0, 에뮬레이터 이벤트 없음). 실제 병원이면 심각한 오탐이므로 (1) 무응답 판정을 소켓 수신 시각 기준으로 분리, (2) 전체의 10 % 이상이 동시에 무응답이면 개별 알람 대신 '시스템 지연' 1건으로 묶기 — 두 항목 제안만 하고 대기 중(사용자 판단). SD 실측(3.3 h, 7,805 rec/s): 쓰기 2.2 MB/s·장치 사용률 20.8 %, 큐 2,000 초과가 전체 분의 17 %, 큐 최고 31,766 = 쓰기 정지 최대 4.1 s. `scripts/stallwatch.py` 추가(스톨 실시간 계측). **SSD Pi 이관 문서 `docs/MIGRATION-SSD.md` 작성**(머신러닝 반영 후 실행 예정; USB 메모리는 대안 아님 — NVMe > USB3+SATA(UASP) 권장). 세션별 WS 지연 지표 추가(`/api/debug/sizes.ws_session_detail`), 정상 종료(SIGTERM 시 저장 플러시), 멀티 뷰어 테스트는 독립 창 열기로 변경.

- [2026-09-21 23:05 RP5] **새 NVMe Pi(hostname `rp5`, 192.168.0.14 / .163, 루트가 `/dev/nvme0n1p2`)에서 라우터 첫 실행**. 새 클론이라 `WITH_NODE=0 scripts/pi-dev-setup.sh`(apt·rustup·릴리스 빌드) + `web/console` `npm ci && npm run build` 후 `scripts/run-router-pi.sh`(저장소 `data/store`, DB `data/router.db` 새로 생성 — 옛 Pi 데이터는 옮기지 않음, systemd 미설치). 시작 5 s 에 ingest 연결 439·채널 550 수신(에뮬레이터가 이미 이 주소로 보내는 중), 드롭 0. 분석 서버 미실행이라 analysis_link 재시도 경고는 정상. 참고: 첫 `cargo test --release` 에서 lib 테스트 1건 실패, 재실행 시 7/7 통과(간헐 실패 — 어느 테스트인지 미확인).

- [2026-09-22 02:05 RP5] **NVMe Pi 저장 스톨 실측: 100분(10분 × 10회) 동안 스톨 0건**. 부하 환자 2,101~2,106·평균 10,590 rec/s(SD 측정 때 7,805 의 1.36배), 저장 큐 p50 0 · p99 130 · p99.9 564 · 최고 772(스톨 기준 2,000), 쓰기 17.95 GB(2.99 MB/s), 디스크 사용률 0.7 %(SD 20.8 %), 라우터 CPU 17~21.5 %. `scripts/stallwatch.py` 가 장치를 자동 선택(mmcblk0 → nvme0n1 → sda, `STALL_DEV` 로 지정). 같은 날 앞서: 뷰어 파형 지연 점검 — 라우터 도착 레코드 나이 120~350 ms, WS 대기 0, 뷰어 표시 지연 1 s(`DISPLAY_DELAY_MS`)는 설계값. 누적 유실 135만 건은 14:45 경 에뮬레이터 환자 세트 재생성·저장소 리셋 구간의 것(이후 0).

- [2026-09-22 16:25 RP5] **저장 파형 백업(설정 › 생체신호 관리)** + 설정 메뉴 분리(설정 › 뷰어 설정 = 기존 그래픽 설정, `#/settings` 는 뷰어 설정으로 연결). 라우터 `src/backup.rs`: 대상 NAS(마운트 경로)·SMB(`smbclient`)·FTP·FTPS·SFTP(`curl`, 자격 증명은 `-K -` 표준 입력), router.db 표 `backup_targets/backup_kv/backup_files/backup_tombs/backup_daily`. 카드 순서 = 우선순위, `copies` 개(1 = 장애 조치, 2+ = 중복) 검증 사본이 생겨야 삭제 가능. 검증 기본 = 원격을 다시 받아 SHA-256 비교(`size` 선택 가능). 삭제: `cap`(기본, 상한 도달 시 백업 완료 파일만, 안 된 파일은 상한 넘어서도 보존) / `immediate`(검증 직후). 디스크 여유 `emergency_free_pct`(5 %) 미만이면 백업 안 된 파일도 삭제 + 사건 `backup_unbacked_delete`. 로컬에서 지운 뒤 같은 이름이 다시 생기면(MCOT 저장 후 전송) `<시간>.late-<ms>.rec` 로 올려 덮어쓰지 않음. 실패 대상은 30 s→5분 백오프 후 다음 순위로. 대상이 없으면 종전과 똑같이 동작. API `/api/backup`(GET 상태), `PUT /policy`, `POST/PUT/DELETE /targets[/id]`, `PUT /order`, `POST /test`(쓰기→다시 읽기 SHA-256→삭제), `POST /scan`. 검증: 로컬 시험 서버(pyftpdlib FTP, asyncssh SFTP, NAS 폴더)로 연결 시험·중복 백업(2 사본 SHA 일치)·1순위 장애 시 2·3순위로 전환·상한 초과 시 백업된 것만 삭제(안 된 1.2 GB 보존)·늦은 파일 `.late` 업로드 확인. **SMB 는 `smbclient` 미설치로 미검증**(sudo 암호 필요 — `sudo apt install smbclient`, `pi-dev-setup.sh` 에 추가). 스캔(파일 4.9만 개) 0.2 s/30 s. **라우터 재시작(새 바이너리, 16:22)**. 같은 날: `scripts/routerwatch.py`(10분 라운드 JSON 감시) 추가, 30일 감시가 17:00 부터 `data/routerwatch.jsonl` 에 기록(분리 실행). 참고: 에뮬레이터 재시작이 반복되면 퇴역 패치 버퍼가 index.json 쓰기 한도(120/분) 대기로 30–60분 늦게 풀림(메모리 +30–50 MB 일시) — 개선안 대기.

- [2026-09-23 12:10 RP5] **레거시 프런트엔드·옛 실행 스크립트 제거**: `web/admin`·`web/viewer`(2026-09-17 이식 뒤 손대지 않음, 죽은 서비스 :7500 에뮬 제어·:7600 db-api 에 묶여 있음)와 이들을 띄우던 `setup-and-run.ps1`·`setup-and-run.bat`·`scripts/start-all.sh`·`scripts/start-all.ps1`·`scripts/stop-all.sh` 삭제. 화면은 `web/console` 하나(라우터가 `dist` 를 :7300 에 서빙), 이 Pi 실행은 `scripts/run-router-pi.sh`. 계기: 사용자가 옛 실행 스크립트를 돌려 5173/5174 의 예전 화면을 봄 — `start-all.sh` 는 시작 시 7300 리스너를 죽이므로 이 Pi 에서 실행하면 운영 중인 라우터가 내려간다. 콘솔이 옛 자산에 의존하지 않음을 확인(평면도는 에뮬레이터 `api.emu.layout()`, 얼굴 이미지는 git 미추적). `analysis-server/`·`db-api/` 는 라우터가 :7100/:7601 로 접속을 시도하므로 유지. `scripts/gen-profiles.py` 는 출력 경로가 없어진 `web/admin/public/faces/` 라 손보거나 지울지 미정. README·PLAN 의 구성 표 갱신, `pi-dev-setup.sh` 의 Node 설명을 콘솔 기준으로 수정.
- [2026-09-23 12:10 RP5] `patch_store::tests::entries_roundtrip_and_detect_corruption` 간헐 실패 해결: 테스트가 닫힌 시간 파일의 1바이트를 뒤집어 손상 검출을 확인하는데, 백그라운드 gzip 워커가 같은 파일을 `.rec.gz` 로 바꿔 버리면 verify 가 멀쩡한 압축본을 읽어 통과해 버렸다. 테스트 저장소를 `with_gzip(.., 0)` 로 만들어 경합 제거, 5회 연속 10/10 통과.

- [2026-09-23 12:35 RP5] **설정 › 네트워크 설정 추가** — 에뮬레이터·분석 서버·DB API 주소를 콘솔에서 지정(`src/netcfg.rs`, router.db `settings` 표, **DB 값 > 환경변수**, 빈 값 저장 시 환경변수로 복귀). 재연결 루프(`emu_link`·`analysis_link`·`db_link`)와 EMR 프록시가 매번 현재 주소를 읽으므로 **재시작 없이 반영**된다. `run_reporter`·`run_emr_sync` 가 주소 미설정 시 시작하자마자 `return` 하던 것을 매 주기 확인으로 바꿔, 나중에 주소를 넣으면 그때부터 붙는다. API `GET/PUT /api/settings/network`, `POST /api/settings/network/test`(에뮬레이터는 `/api/v1/status` 조회해 전송 상태·pkt/s·대상까지 표시, 나머지는 TCP 접속). 계기: 다른 PC 에서 저장소만 받아 실행하니 도면이 `HTTP 503`(=`ROUTER_EMULATOR_ADDR` 미설정) 으로 실패 — 503 본문도 "설정 › 네트워크 설정에서 지정하세요"로 한국어 안내, 미설정 시 시작 로그에 경고. 검증: 정상/불통 주소 연결 시험, 형식 오류 거부, 틀린 주소 저장 시 layout 502 → 빈 값 저장 후 200 복귀, 라우터 재시작 1회(새 바이너리) 뒤 환자 2,100 재연결·드롭 0.

- [2026-09-23 13:40 RP5] 네트워크 설정에서 **포트 생략 허용**: IP·호스트 이름만 넣으면 대상별 기본 포트를 붙여 저장한다(에뮬레이터 5445, 분석 7100, DB 7601). 저장·연결 시험 모두 적용되고 화면에도 안내 표시. IPv6 는 `[::1]:5445` 형태를 요구하도록 함께 정리(대괄호 없는 `::1` 은 거절 — 종전 `rsplit_once(':')` 은 `::1` 을 호스트 `:` + 포트 `1` 로 잘못 읽었다). 라우터 재시작(새 바이너리), 환자 2,100 재연결·드롭 0.

- [2026-09-23 17:10 RP5] **병원 지도 도면 재작업** — 단색 폴리곤을 공공 건물 안내도(wayfinding) 스타일로 다시 그림(`web/console/src/pages/FloorPlan.jsx` 신규, MapPage 는 데이터·선택·사이드패널만). 건축 도면 관례: 벽 poche(방 윤곽 0.26, 외벽 0.5), **문은 벽을 끊고 열림 방향 호**(layout 의 `door` N/S/E/W), 침대 픽토그램(매트리스+베개+이불선, `angle` 회전, 재실 시 강조), 계단 디딤판·승강기 대각선, 설비 아이콘(중앙 모니터는 강조색), 수직동선 빗금. 방 용도 19종을 10계열로 묶어 파스텔 색 + 범례, 축척 막대·방위표, 휠 확대/드래그 이동/전체보기. 이름표는 방 크기에 맞춰 글자 크기를 정하고 넘치면 자르며(`02 격리(음압)` → 번호 크게 + 설명 아랫줄), 가구 위에서도 읽히도록 `paint-order` 바깥선. 알람은 방을 통째로 칠하지 않고 옅은 물들임 + 테두리 강조(용도색이 묻히지 않게). 라이트/다크 팔레트 각각 조정(다크는 채도 낮춤). `#/map?b=0&f=3` 딥링크 추가. 검증: 헤드리스 크로뮴으로 병동층·검사층(124 m)·다크 테마 스크린샷 확인. 참고: 이 Pi 에는 한글 폰트가 없어 헤드리스 캡처에서만 글자가 네모로 보인다(실제 브라우저는 정상).

- [2026-09-23 18:10 RP5] 병원 지도 **방 클릭 시 환자 목록이 안 뜨던 버그** 수정 + **겹침 정리**. 원인: 새 도면의 끌어 이동이 누르는 순간 `setPointerCapture` 해서 방·환자·게이트웨이의 click 이 wrapper 로 가 버림 → 4 px 넘게 움직여 끌기가 확정된 뒤에만 가로채고, 끌기 직후의 click 은 한 번 버림. 겹침: 배율별 단계 표시(LOD — 방 번호는 항상, 용도 부제는 14 px/m, 정상 게이트웨이는 22, 환자 이름은 34 부터; 이상 게이트웨이는 항상), 침대 있는 방은 "병실" 부제·인원 배지 생략(침대 위 점이 대신), 환자 점은 베개·이름은 이불 위(`bedPoint`), 게이트웨이는 방 오른쪽 위 모서리로 옮기고 복도 것은 같은 자리 모니터와 겹치면 옆으로. 이름표는 가운데/좌/우 × 세로 5단 후보 중 침대·설비와 덜 겹치는 자리를 고르고(간호사실은 데스크 아래로), 긴 이름은 '/'·공백에서 두 줄로 줄바꿈("직원휴게실/회의 A"), 번호 분리는 숫자로 시작하는 이름만. 폭에 딱 맞는 글자 수가 부동소수점으로 한 글자 잘리던 버그(`엘리베이터…`)도 수정. 검증: puppeteer-core(임시 폴더)로 방 클릭 → 사이드패널 환자 4명, 끌기 후 선택 유지, 전체·중간·근접 배율 캡처 확인. 캡처용으로 한글 폰트(Noto Sans KR TTF)를 사용자 폰트 폴더 `~/.local/share/fonts/noto-kr` 에 넣음(시스템 변경 없음) — 이제 이 Pi 의 브라우저에서도 한글이 보인다.

- [2026-09-23 18:55 RP5] **에뮬레이터 새 도면 규칙 반영**(에뮬레이터 HANDOFF 2026-09-23 18:05 "라우터 뷰어는 침대 angle 과 게이트웨이 x·y 를 반영해야 함"). 에뮬레이터 소스(`emulator/hospital/layout.py` `bed_positions`·`door_segment`, `hospital.py` `_gw_spots_from_beds`)로 규칙 확인: 침대 `angle` = **머리 방위**(0 북·90 동·180 남·270 서), 1.08 × 2.4 m, 머리는 양옆 벽·발은 가운데 통로 — 이전 렌더러는 angle 0 을 가로 침대로 봐서 **모든 침대를 90° 틀리게** 그리고 있었다(옛 데이터도 같은 규칙이었음). 문은 새 필드 `door_seg`(복도측 벽 가운데 1.2 m)를 그대로 쓰고 문짝은 방 안쪽으로. 게이트웨이는 천장 실제 좌표(환자 상체 무게중심)에 그림 — 방 모서리로 옮기던 것 제거, `mount: mobile` 은 평면에서 제외. **라우터 변경**: `Patient.bed` 추가, EMR admissions 의 `bed`(예 `106A01-A`)를 받아 환자를 실제 배정 침대에 놓음(없거나 다른 방 침대면 빈 침대 순서). 이름표는 게이트웨이 자리도 피하고, 잘릴 바엔 글자를 줄여 가운데에 넣음. 라우터 재시작(새 바이너리), 입원 환자 2,000명 모두 침대 id 수신 확인.

- [2026-09-23 20:05 RP5] 병원 지도: **게이트웨이 커버리지·에뮬레이터식 환자 표식·검색·클릭 확대** (에뮬레이터 `emulator/web/static/app.js` 이식). 커버리지 = 광선 240개, 트인 곳 ~10 m, 벽 하나마다 남은 거리 절반, 층 외곽에서 멈춤(벽 = 모든 방 윤곽). 게이트웨이에 마우스 → 반투명 영역 + 10 m 점선 원, '커버리지' 버튼 → 모든 게이트웨이 영역을 도려낸 나머지를 회색 빗금(음영지역). 환자 = 사람 아이콘(RSSI 색: 양호/보통/약함/매우 약함/끊김) + 리드오프 점 + 이름 항상 표시(머리 쪽에 아이콘, 이름은 통로 쪽). 이름은 자르지 않음: 마주 보는 두 이름은 통로를 나눠 쓰고(짧은 쪽은 제 크기), 벽·침대에 닿으면 반대쪽으로 옮기고(벽은 넘지 않음), 그래도 모자라면 그 이름만 작게. 검색 = 환자 이름(한글 자모 부분 일치)·MRN·패치·환자 id, 게이트웨이 #번호·ID·방 → 고르면 그 층으로 이동 + 빨간 맥동 링 + 2.5배 확대, 검색어를 지우면 원래대로. **휠 확대 제거** — 빈 곳(방·환자·게이트웨이가 아닌 곳) 클릭 = 그 지점 2.5배 확대 ↔ 다시 클릭 = 원래 배율, 끌기 이동은 확대 상태에서만(에뮬레이터와 같음). 방 클릭은 지금처럼 환자 목록. 검증: puppeteer 시나리오(휠 무반응, "김은" 입력 → 9층 이동·링·확대, 지우면 복귀, 빈 곳 클릭 확대/복귀, 방 클릭 목록 4명).

- [2026-09-23 20:50 RP5] 병원 지도 **'표시' 메뉴**(에뮬레이터 평면도와 같은 4모드, `localStorage map.mode`): 환자 + 게이트웨이 + 설비(기본) / 게이트웨이 상태(환자 숨김, 모든 게이트웨이, 바깥 호 = 연결 패치/최대(capacity), `#번호`) / 게이트웨이 음영지역(상태 표기 + 음영 빗금) / 도면만(설비·게이트웨이·환자 숨김). 게이트웨이 아이콘은 사용자 요청대로 **모든 모드에서 기존 와이파이 원 아이콘** — 에뮬레이터식 부채꼴 아이콘은 쓰지 않음. 예전 '커버리지' 버튼 설정(`map.cov=1`)은 음영지역 모드로 이어받음. 검색창에 **× 삭제 버튼**(검색어·하이라이트·확대 원래대로). 검증: 모드별 요소 수(환자 46/0/0/0, 부하 호 0/16/16/0, 설비 12/12/12/0, 음영 0/0/1/0), × 클릭 후 링 0·확대 해제.

- [2026-09-23 21:05 RP5] 병원 지도 검색 하이라이트: 빨간 맥동 링은 **5초 뒤 사라짐**(확대·검색어는 유지). 다른 결과를 고르면 다시 5초.

- [2026-09-23 21:40 RP5] 병원 지도 표기 정리: ① **병실 이름표 = 층+호실 + 구역** — 한 층에 A·B 병동이 같은 호실 번호를 쓰므로 id `103A01`(건물·층·병동·호실)에서 "301호" + "3A병동"(격리실은 "3A병동 격리(음압)")을 만들어 항상 표시(병실만, 다른 방은 그대로). ② **게이트웨이 번호** — 숫자만 아이콘 아래 작게, 게이트웨이가 보이는 모든 모드. ③ **게이트웨이는 에뮬레이터 좌표 그대로** — 복도 모니터와 겹칠 때 옆으로 비켜 그리던 보정 제거(물리적 위치를 표기 때문에 옮기지 않음, 이름표가 게이트웨이를 피함). ④ **호실 표기는 출입문 가까이** — `door_seg` 에서 문 여닫이 범위를 장애물로 비우고, 이름표 자리 점수에 문까지 거리를 넣음.

- [2026-09-23 21:55 RP5] 병원 지도 호실 글자 크기 통일: 층마다 가장 좁은 병실에 맞는 크기 하나(최대 1.1 m)를 모든 병실 호실에 쓰고, 구역 줄("3A병동")도 그 58 % 한 크기(넘치면 공백에서 줄바꿈, 크기는 유지). 그 크기가 안 들어가는 칸은 자리 후보에서 제외. 3층 0.95 m, 13층 0.76 m("1301호" 5글자).

## 4. MAC → RP5#2 전달 사항

- [2026-09-17 23:20 MAC] 처음 설치할 때: `git clone` → `scripts/pi-dev-setup.sh` → `ROUTER_STORE_DIR` 를 SSD 마운트 아래로 두고 실행. systemd 유닛은 P4 에서 `deploy/pi/` 로 만들 예정이니, 그 전에 필요하면 임시로 만들고 여기에 적어 주세요.

## 5. 변경 로그 (최신이 아래)

- [2026-09-17 22:50 MAC] 라우터 P1 구현·검증 (48a3480).
- [2026-09-17 23:20 MAC] RP5#2 이어 개발 준비: Linux sysmon(/proc), `rust-toolchain.toml`, `scripts/pi-dev-setup.sh`, `docs/RP5-DEV.md`, `CLAUDE.md`, 이 문서.
